import { StyleFunctionProps } from "@chakra-ui/react";

const inputTheme = {
  baseStyle: (props: StyleFunctionProps) => ({
    field: {
      borderRadius: "xl",
    },
  }),
};
export { inputTheme };
